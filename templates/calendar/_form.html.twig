  <div class="container">
    {{ form_start(form) }}
    <div class="row mt-4">
        <div class="col-12 mb-2 px-2">
            <div class="form-floating" style="overflow: hidden;  text-overflow: clip; white-space: nowrap;">
                {{form_widget(form.title)}}
                {{form_label(form.title)}}
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-2 mt-3 px-2">
            <div class="form-floating" style="overflow: visible;  text-overflow: clip; white-space: nowrap;">
                <p>Heure de début</p>
                {{form_widget(form.start)}}
            </div>
        </div>
    </div>
    <div class="row ">
        <div class="col-2 mt-3 px-2">
            <div class="form-floating" style="overflow: visible;  text-overflow: clip; white-space: nowrap;">
                <p>Heure de fin</p>
                {{form_widget(form.end)}}
            </div>
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-2 mb-2 px-2">
            <div class="form-floating" style="overflow: hidden;  text-overflow: clip; white-space: nowrap;">
                {{form_widget(form.category)}}
                {{form_label(form.category)}}
            </div>
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-12 mb-2 paddingdescription">
            <div class="form-floating" style="overflow: hidden;  text-overflow: clip; white-space: nowrap;">
                {{form_widget(form.description)}}
                {{form_label(form.description)}}
            </div>
        </div>
        <div class="d-grid gap-2 col-2">
                <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
        </div>
        <div class="d-grid gap-2 col-2">
                <a href="{{ path('main') }}" class="btn btn-secondary">Retour</a>
        </div>
    {{ form_end(form) }}
    </div>
</div>
